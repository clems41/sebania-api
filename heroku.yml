build:
  docker:
    sebania: Dockerfile
run:
  web:
    command:
      - gunicorn --bind 0.0.0.0:$PORT --workers 3 sebania.wsgi:application
    image: sebania
  worker:
    command:
      - python manage.py process_tasks
    image: sebania